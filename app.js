const PUSH_API="https://saisip-push.ivansimango02.workers.dev";const VAPID_PUBLIC_KEY="BLNtPE0kfrRsYktofuo1M19IdnJG3kuAZPWAP2RDDbI85-BIb1RqCi1gcybGwINykamVCMlc-CM7KyRTwV0UDA0";const $=s=>document.querySelector(s),K="saisip-v4",today=()=>new Date().toLocaleDateString("en-CA");let data=JSON.parse(localStorage.getItem(K)||"null")||{started:today(),goal:2000,interval:60,start:"08:00",end:"22:00",days:{}};function day(){return data.days[today()]||(data.days[today()]={amount:0,times:[]})}function save(){localStorage.setItem(K,JSON.stringify(data));render()}function streak(){let n=0,d=new Date();for(let i=0;i<365;i++){let k=d.toLocaleDateString("en-CA"),x=data.days[k];if(x&&x.amount>=data.goal)n++;else if(i>0)break;d.setDate(d.getDate()-1)}return n}function history(){let b=$("#historyList");b.innerHTML="";for(let i=0;i<7;i++){let d=new Date();d.setDate(d.getDate()-i);let k=d.toLocaleDateString("en-CA"),a=data.days[k]?.amount||0,p=Math.min(100,a/data.goal*100);b.insertAdjacentHTML("beforeend",`<div class="day"><b>${i?d.toLocaleDateString(undefined,{weekday:"short"}):"Today"}</b><div class="daybar"><i style="width:${p}%"></i></div><em>${a} ml</em></div>`)}}function render(){let d=day(),p=Math.min(100,Math.round(d.amount/data.goal*100)),h=new Date().getHours(),g=h<12?"Good morning":h<18?"Good afternoon":"Good evening";$("#greeting").textContent=`${g}, Sai 💗`;$("#amount").textContent=`${d.amount.toLocaleString()} ml`;$("#goalText").textContent=data.goal.toLocaleString();$("#percent").textContent=p+"%";$("#bar").style.width=p+"%";$("#glasses").textContent=d.times.length;$("#streak").textContent=streak()+" 🔥";$("#cheer").textContent=p>=100?"Goal complete! Hydration queen 👑💧":p>=50?"Halfway there, Sai 💕":"Let's get hydrated, Sai! 💪💖";$("#goal").value=data.goal;$("#interval").value=data.interval;$("#start").value=data.start;$("#end").value=data.end;$("#started").textContent=new Date(data.started+"T12:00:00").toLocaleDateString(undefined,{day:"numeric",month:"long",year:"numeric"});history()}$("#drink").onclick=()=>{let d=day();d.amount+=250;d.times.push(new Date().toISOString());save()};$("#undo").onclick=()=>{let d=day();if(d.times.length){d.times.pop();d.amount=Math.max(0,d.amount-250);save()}};$("#gift").onclick=()=>alert("Drink water, Sai 💗\nIvan loves you. Also stop ignoring the reminders 😂💧");document.querySelectorAll(".nav").forEach(b=>b.onclick=()=>{document.querySelectorAll(".nav,.page").forEach(x=>x.classList.remove("active"));b.classList.add("active");$("#"+b.dataset.page).classList.add("active")});$("#saveSettings").onclick=()=>{data.goal=+$("#goal").value||2000;data.interval=+$("#interval").value||60;data.start=$("#start").value||"08:00";data.end=$("#end").value||"22:00";save();alert("Saved 💗")};function key(s){let p="=".repeat((4-s.length%4)%4),b=(s+p).replace(/-/g,"+").replace(/_/g,"/"),r=atob(b),a=new Uint8Array(r.length);for(let i=0;i<r.length;i++)a[i]=r.charCodeAt(i);return a}async function push(){try{if(VAPID_PUBLIC_KEY.includes("PASTE_"))throw Error("Add your VAPID public key to app.js first.");let reg=await navigator.serviceWorker.register("./sw.js");await navigator.serviceWorker.ready;if(await Notification.requestPermission()!=="granted")throw Error("Notifications were not allowed.");let sub=await reg.pushManager.getSubscription()||await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:key(VAPID_PUBLIC_KEY)});let r=await fetch(PUSH_API+"/subscribe",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({subscription:sub,settings:{enabled:true,interval:data.interval,start:data.start,end:data.end,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone}})});if(!r.ok)throw Error("Couldn't save subscription.");$("#pushState").textContent="Notifications are on 💗";$("#enablePush").textContent="Enabled"}catch(e){alert(e.message)}}$("#enablePush").onclick=push;let jokes=["Your body needs water, not more overthinking 😂💧","Saiiii 😭 the water is waiting for you.","Hydrate first. Be dramatic later 😂💕","Ivan says drink water. Apparently love now comes with hydration reminders 💀💧"];$("#funny").textContent=jokes[Math.floor(Math.random()*jokes.length)];if("serviceWorker"in navigator)navigator.serviceWorker.register("./sw.js").catch(()=>{});render();